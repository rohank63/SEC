os:
- linux
dist: bionic
language: python

services:
- docker

env:
  global:
  - VERSION: 0.0.2

stages:
- name: test
- name: print
  if: branch = master


jobs:
  include:
  - stage: test
    name: Run style, unit and integration tests
    script:
    - python scr/file.py
    - python a.py
    - docker build -t img .
  - stage: print
    name: testing branch conditioin
    script:
    - python a.py



